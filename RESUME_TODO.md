# TODO before the site is fully live

- [x] ~~`resume.pdf`~~ — added (`resume.pdf` in repo root; buttons download it as `Aayush_Kumar_Resume.pdf`).
- [x] ~~`aayush.jpg`~~ — added (optimized to 552×1200, ~137 KB; powers the hero and the OG/Twitter card).

## Still to do

1. **Formspree ID** — create a free account at [formspree.io](https://formspree.io), create a form,
   and replace `FORMSPREE_ID` in `index.html` (one spot, in the contact form's `action` attribute)
   with your real form ID. Until then the form's error state points visitors to your email.

2. **Project links** — each project card has an `href="#"` link with a `data-todo` attribute and an
   HTML comment listing what to supply (public repo URL, TestFlight link, or screenshots page).
   The MCP servers card must point to a clean-room public rebuild — not IBM code.

Delete this file once done.
